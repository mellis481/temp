import { VtxConfig } from '@vertexinc/vtx-ui-react-component-library';
import React from 'react';

// This component isn't needed. It's here only to satisfy the rules in vtx-react-scripts
export const Root: React.FC = (props) => {
  return <VtxConfig identifier="vtx-ui-mf-test"></VtxConfig>;
};
